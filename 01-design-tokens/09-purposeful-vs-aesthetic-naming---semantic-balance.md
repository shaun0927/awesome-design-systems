# Purposeful vs Aesthetic Naming - Semantic Balance

> Original issue: shaun0927/stocktitan-crawler#590

## 📌 핵심 개념

- **Purposeful Naming**: 사용 목적 기반 (예: `primary-action`, `error-message`) - 의도가 명확하지만 유연성 제한
- **Aesthetic Naming**: 시각적 특성 기반 (예: `blue-500`, `large`) - 유연하지만 의미 불명확
- **하이브리드 접근법**: 두 방식의 장점을 결합하여 상황에 맞게 선택
- **컨텍스트가 핵심**: 같은 컴포넌트도 사용 맥락에 따라 다른 이름이 적절할 수 있음

## 🎯 실무 노하우

- **색상 토큰 전략**: `color.brand.primary` (목적) + `color.blue.500` (심미) 병행 사용
- **컴포넌트 Variants**: 기능적 이름 (`destructive`, `constructive`) 우선, 시각적 이름 (`outlined`, `filled`) 보조
- **일관성 우선**: 한 시스템 내에서는 하나의 네이밍 철학을 일관되게 적용
- **마이그레이션 고려**: 디자인 변경 시 목적 기반 이름이 더 안정적 (색상이 바뀌어도 이름 유지)
- **팀 합의 필수**: 네이밍 규칙은 문서화하고 팀 전체가 동의해야 함

## 📊 네이밍 비교

```
┌─────────────────┬─────────────────┬─────────────────┐
│ 유형            │ Purposeful      │ Aesthetic       │
├─────────────────┼─────────────────┼─────────────────┤
│ 색상            │ error, success  │ red-500, green  │
│ 버튼            │ primary-action  │ large-blue      │
│ 간격            │ content-gap     │ spacing-4       │
│ 장점            │ 의도 명확       │ 유연성          │
│ 단점            │ 경직성          │ 의미 불명확     │
└─────────────────┴─────────────────┴─────────────────┘
```

---
> 출처: Nathan Curtis (EightShapes)