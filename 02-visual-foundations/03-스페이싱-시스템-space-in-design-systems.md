# 스페이싱 시스템 (Space in Design Systems)

> Original issue: shaun0927/stocktitan-crawler#569

# Space in Design Systems: 6가지 핵심 개념 (Nathan Curtis, 2016)

## 📌 핵심 개념

### Space의 중요성
- **CSS 속성 사용 빈도**: Space 관련 속성이 Color 다음으로 가장 많이 사용됨
- **소통의 장벽**: 디자이너-개발자 간 가장 큰 갈등 요소
- **Red-line Specs의 폐해**: 픽셀 단위 수작업 표시는 비효율적

### Space 기본 원칙

#### 1. Grid ≠ Space
- **Grid는 Space를 사용하는 컴포넌트일 뿐**
- Grid의 margin, gutter 값을 더 깊은 공간 개념과 정렬할 것

#### 2. Base Number는 16
```
Base 16을 권장하는 이유:
- 기본 폰트 사이즈로 적합
- 모든 화면 해상도(320, 768, 1024)의 약수
- 배수(32, 64) 및 약수(8, 4, 2)가 기억하기 쉬움
```

#### 3. 비선형 스케일 사용
```scss
// 기하급수적 진행 (Geometric Progression)
$space-xxs: 2px;   // base / 8
$space-xs:  4px;   // base / 4
$space-s:   8px;   // base / 2 (25% 사용)
$space-m:   16px;  // base (41% 사용)
$space-l:   32px;  // base * 2 (21% 사용)
$space-xl:  64px;  // base * 4 (4% 사용)
```

#### 4. T-shirt 사이즈로 네이밍
```
XXS, XS, S, M (기준점), L, XL, XXL
```

## 🎯 6가지 Space 개념

### Concept 1: Inset (정사각형 인셋)
```
┌──────────────────┐
│  ←  padding  →   │
│  ┌────────────┐  │
│ ↑│  Content   │↑ │
│ ↓│            │↓ │
│  └────────────┘  │
└──────────────────┘
```
- **정의**: 4방향 모두 동일한 padding
- **사용 사례**: Card, Block message, Footer
- **반응형**: Mobile First → 768px+ 에서 Large로 확장

### Concept 2: Squish Inset (압축된 인셋)
```
┌──────────────────┐
│ ←     wide    → │
│ ┌────────────┐  │
↕ │  Content   │  │ ← 세로 50% 감소
│ └────────────┘  │
└──────────────────┘
```
- **정의**: 상하 padding을 50% 줄임
- **사용 사례**: Button, Data table cells, List items

### Concept 3: Stretch Inset (늘어난 인셋)
```
┌──────────────────┐
│ ← narrow →       │
│ ┌────────────┐   │
↕ │            │   │ ← 세로 증가
↕ │  Content   │   │
│ └────────────┘   │
└──────────────────┘
```
- **정의**: 세로 padding 증가
- **사용 사례**: Textbox, Textarea, Form elements

### Concept 4: Stack (수직 스택)
```
┌────────────────┐
│  Element 1     │
├────────────────┤ ← margin-bottom
│  Element 2     │
├────────────────┤ ← margin-bottom
│  Element 3     │
└────────────────┘
```
- **정의**: 세로로 쌓을 때 사용하는 margin
- **압도적 사용 빈도**: 대부분의 UI가 세로 스크롤
- **사용 사례**: Card 내부 섹션, Form 필드, Module 간 간격

### Concept 5: Inline (인라인 배치)
```
[Tag 1] [Tag 2] [Tag 3]
[Tag 4] [Tag 5]
```
- **정의**: 좌우로 흐르며 자동 줄바꿈되는 요소 간 간격
- **사용 사례**: Pills, Tags, Breadcrumbs, Footer links

### Concept 6: Grid (그리드)
- **정의**: Grid의 margin과 gutter를 전체 공간 시스템과 정렬
- **타이밍**: 공간 시스템이 안정화된 후 Grid를 재검토

## 💡 실무 구현 예시

### Phase 1: Foundation
```scss
$space-base: 16px;

$space-xxs: 2px;
$space-xs:  4px;
$space-s:   8px;
$space-m:   16px;
$space-l:   32px;
$space-xl:  64px;
```

### Phase 2: Concepts
```scss
// Inset (정사각형)
$space-inset-m:   $space-m;

// Inset Squish (세로 50%)
$space-inset-squish-m:  $space-s $space-m;  // 8px 16px

// Stack (수직 간격)
$space-stack-m:   0 0 $space-m 0;

// Inline (수평 간격)
$space-inline-m:  0 $space-m 0 0;
```

### Phase 3: Application
```scss
.card {
  padding: $space-inset-m;  // 16px 전방향
  margin: $space-stack-m;   // 아래 16px

  &__title {
    margin: $space-stack-m;
  }

  &__button {
    padding: $space-inset-squish-m;  // 8px 16px
  }
}
```

## 🎨 Card 컴포넌트 실전 분석

```
Card Component 구조:
┌─────────────────────────────────┐
│ 1. .card (container)            │ ← Inset M
│   ┌─────────────────────────┐   │
│   │ 2. Type label           │   │ ← Stack S
│   │ 3. Title                │   │ ← Stack M
│   │ 4. Description          │   │ ← Stack M
│   │ 5. [Pills]              │   │ ← Inset S
│   │   [Pill] [Pill]         │   │ ← Inline S
│   │ 6. [Button]             │   │ ← Inset Squish M
│   └─────────────────────────┘   │
└─────────────────────────────────┘
```

## 📊 실무 적용 효과

### Before (시스템 없이)
```css
.card { padding: 22px; margin-bottom: 18px; }
.title { margin-bottom: 14px; }
```
**문제**: 재사용 불가, 소통 비용 ↑, QA 논쟁

### After (시스템 적용)
```scss
.card {
  padding: $space-inset-m;      // 16px
  margin-bottom: $space-stack-m; // 16px
}
```
**효과**:
- 패턴 명확 → 재사용 ↑
- 디자이너도 이해 가능 → 소통 비용 ↓
- QA 간소화 → "시스템 규칙대로만 확인"

## 🚀 고급 활용: Density Control

```scss
// Base System
$space-m: 16px;

// Compact Theme
$space-m-compact: 12px;  // 25% 감소

.theme-compact {
  .card {
    padding: $space-inset-m-compact;
  }
}
```

## 🎓 핵심 Takeaway

1. **시각적으로 가르쳐라**: Cheatsheet 제공
2. **Generic Helper도 제공하되 모니터링**
3. **변수명에 Padding/Margin 넣지 마라**: 개념 기반 네이밍
4. **Line Height 충돌을 시스템적으로 해결**
5. **Density를 Space 개념으로 조절**

---
*출처: Nathan Curtis (EightShapes, 2016)*