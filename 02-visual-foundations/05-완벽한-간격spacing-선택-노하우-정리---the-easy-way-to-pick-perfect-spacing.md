# 완벽한 간격(Spacing) 선택 노하우 정리 - The Easy Way to Pick Perfect Spacing

> Original issue: shaun0927/stocktitan-crawler#513

# 완벽한 간격(Spacing) 선택 노하우 정리

> 출처: "The Easy Way to Pick Perfect Spacing" 영상 transcript 분석
> 관점: 디자인 시스템 효율성 및 실전 노하우

---

## 📌 핵심 철학

### 1. Rem 단위 사용의 중요성
- **픽셀 대신 rem을 사용하는 이유**: 폰트 크기에 따라 간격이 자동으로 스케일됨
- **기본값**: 1rem = 16px
- **장점**: 일관되고 확장 가능한 간격 시스템 구축 가능

### 2. 간격의 본질적 역할
> **"Grouping and separating UI elements to help users navigate the interface"**

- 간격은 단순히 "공간"이 아니라 **정보 위계와 그룹핑을 전달하는 핵심 도구**
- 동일한 간격 = 혼란스러운 UI (사용자가 어느 요소가 어느 그룹에 속하는지 고민해야 함)
- **원칙**: 가장 작은 간격으로 그룹을 만들고, 서로 다른 그룹 간에는 1rem 이상 차이

### 3. 일관성의 힘
> **"Even if the number is not right, if you stay consistent, the overall design will look okayish."**

- 잘못된 값이라도 일관되게 사용하면 디자인이 완전히 망가지지 않음
- 하지만 **올바른 값 + 일관성**이 최고의 조합

---

## 🎯 간격 시스템 설계

### 4px/8px 그리드 기반 Spacing Scale

| Spacing Token | rem | px (16px 기준) | 주요 용도 |
|---------------|-----|----------------|-----------|
| `spacing-0` | 0rem | 0px | 요소 밀착 |
| `spacing-1` | 0.25rem | 4px | 최소 간격 (아이콘-텍스트) |
| `spacing-2` | 0.5rem | 8px | 밀접한 관계 요소 (제목-부제) |
| `spacing-3` | 0.75rem | 12px | 작은 padding |
| `spacing-4` | **1rem** | **16px** | 🔥 **기본 간격** (버튼 그룹, 리스트 아이템) |
| `spacing-5` | 1.25rem | 20px | 여유 있는 padding (수직 요소 많을 때) |
| `spacing-6` | **1.5rem** | **24px** | 🔥 **그룹 분리** (서로 다른 섹션) |
| `spacing-8` | **2rem** | **32px** | 🔥 **섹션 간 여백** |
| `spacing-10` | 2.5rem | 40px | 큰 섹션 분리 |
| `spacing-12` | 3rem | 48px | 메인 섹션 분리 |

**핵심 3가지 값** (80% 케이스 커버):
- **0.5rem (8px)**: 밀접한 관계 요소
- **1rem (16px)**: 기본 간격 (버튼 그룹, padding)
- **1.5rem (24px)**: 그룹 분리

---

## 🛠️ 실전 규칙

### Rule 1: 안쪽 간격 < 바깥쪽 간격 (Inner < Outer)

```
❌ 잘못된 예:
┌─────────────────────┐
│  [Icon  Text]       │  ← 아이콘-텍스트 간격이 padding보다 크면 버튼이 어색함
└─────────────────────┘

✅ 올바른 예:
┌─────────────────────┐
│   [Icon Text]       │  ← 내부 간격(0.5rem) < 좌우 padding(1rem)
└─────────────────────┘
```

| 요소 | 내부 간격 | 외부 padding | 비율 |
|------|----------|--------------|------|
| 버튼 (아이콘+텍스트) | 0.5rem | 1rem (좌우) | 1:2 |
| 카드 내부 요소 | 0.75rem | 1rem | 1:1.33 |
| 복잡한 컨테이너 | 1rem | 1.5~2rem | 1:1.5~2 |

**예외**: 서로 다른 목적의 요소는 내부 간격이 클 수 있음
(예: Like/Dislike 버튼 그룹 내부 간격 > 각 버튼의 padding)

---

### Rule 2: 광학적 무게(Optical Weight) - 수직 vs 수평 padding

#### 문제 인식
- **텍스트의 시각적 노이즈는 좌우가 더 많음** (U vs W 같은 다양한 글자 너비)
- **수직 공간은 cap height와 descender로 제약됨** (Y, G의 꼬리 부분)
- → 동일한 padding을 적용하면 버튼이 "bloated"하게 보임

#### 해결책

| 요소 유형 | 수직 padding | 수평 padding | 비율 |
|-----------|--------------|--------------|------|
| 단순 버튼 (텍스트만) | 0.5rem | 1rem | 1:2 |
| 버튼 (아이콘+텍스트) | 0.5rem | 1.5rem | 1:3 |
| 수직 요소 많은 카드 | 1.25rem | 1.25rem | 1:1 |

**결정 기준**:
- 수평 흐름 위주 → 수평 padding 2~3배
- 수직 요소 많음 → 동일 padding (광학적 균형 위해)

```css
/* 버튼 예시 */
.button {
  padding: 0.5rem 1rem; /* 수직:수평 = 1:2 */
}

/* 수직 요소 많은 카드 */
.card-with-vertical-content {
  padding: 1.25rem; /* 동일 padding으로 숨 쉴 공간 제공 */
}
```

---

### Rule 3: 큰 값에서 시작해서 줄이기 (Start Big, Then Reduce)

```
❌ 잘못된 접근:
0.5rem → 너무 작네? → 0.75rem → 여전히 작네? → 1rem

✅ 올바른 접근:
1.5rem → 너무 크네? → 1rem → 적당함!
```

**이유**:
- 작은 간격은 사용자 경험을 **실질적으로 해침** (가독성 저하)
- 큰 간격은 단지 "여유로워 보임" (공간이 있다면 사용하라)

#### 실전 워크플로우

| 단계 | 작업 | 시작값 |
|------|------|--------|
| 1 | 섹션 padding 설정 | 2rem (너무 크면 1.5rem으로 조정) |
| 2 | UI를 그룹으로 나누기 | - |
| 3 | 그룹 간 분리 | 1.5rem (또는 2rem) |
| 4 | 그룹 내부 요소 간격 | 1rem 시작 → 필요시 0.75rem, 0.5rem으로 감소 |
| 5 | 밀접한 관계 요소 | 0.5rem 이하 |

---

### Rule 4: 그룹핑 전략

#### Step 1: UI를 그룹으로 분해

```
섹션 예시:
┌─────────────────────────────┐
│ [제목]                       │ ← 그룹 1
│ [설명 텍스트]                │
├─────────────────────────────┤
│ [옵션 1]                     │ ← 그룹 2
│ [옵션 2]                     │
│ [옵션 3]                     │
├─────────────────────────────┤
│ [취소] [확인]                │ ← 그룹 3
└─────────────────────────────┘
```

#### Step 2: 간격 값 할당

| 관계 | 간격 값 | 예시 |
|------|---------|------|
| 매우 밀접 (제목-부제) | 0.25~0.5rem | "Title" ↔ "Subtitle" |
| 같은 그룹 내부 | 0.75~1rem | 리스트 아이템, 옵션들 |
| 그룹 간 분리 | 1.5~2rem | 옵션 그룹 ↔ 버튼 그룹 |
| 섹션 간 분리 | 2~3rem | 메인 섹션들 |

```css
/* 그룹핑 예시 */
.modal-section {
  padding: 1.5rem; /* 외부 여백 */
  display: flex;
  flex-direction: column;
  gap: 1.5rem; /* 그룹 간 간격 */
}

.group-header {
  display: flex;
  flex-direction: column;
  gap: 0.5rem; /* 제목-부제 간격 */
}

.group-options {
  display: flex;
  flex-direction: column;
  gap: 1rem; /* 옵션 간 간격 */
}

.group-actions {
  display: flex;
  gap: 1rem; /* 버튼 간 간격 */
  justify-content: space-between;
}
```

---

### Rule 5: 간격과 padding의 균형

> **"We usually put the same outer spacing or gap as the inner spacing or padding for a better visual balance."**

```
카드 예시:
┌─────────────────────────┐
│  padding: 1rem          │ ← 외부 padding
│  ┌───────────────────┐  │
│  │ 내부 요소         │  │
│  │ gap: 1rem         │  │ ← 내부 gap도 1rem
│  └───────────────────┘  │
└─────────────────────────┘
```

**시각적 균형을 위한 1:1 비율**:
- 카드 padding = 1rem → 내부 요소 gap = 1rem
- 버튼 수평 padding = 1rem → 버튼 간 gap = 1rem

---

### Rule 6: Border Radius와의 조화

**간격 값과 border-radius를 맞추면 광학적으로 균형잡힌 디자인**:

| Padding | Border Radius | 조합 |
|---------|---------------|------|
| 0.5rem | 0.5rem | 작은 버튼, 태그 |
| 1rem | 1rem | 기본 버튼, 인풋 |
| 1.5rem | 1.5rem | 카드 |
| 2rem | 2rem | 큰 섹션 |

```css
/* 조화로운 예시 */
.card {
  padding: 1rem;
  border-radius: 1rem; /* padding과 동일 */
}

.button {
  padding: 0.5rem 1rem;
  border-radius: 0.5rem; /* 수직 padding과 동일 */
}
```

---

## 💻 코드 예시

### CSS Custom Properties (Spacing Tokens)

```css
:root {
  /* Spacing Scale (4px 단위) */
  --spacing-0: 0rem;
  --spacing-1: 0.25rem;  /* 4px */
  --spacing-2: 0.5rem;   /* 8px - 밀접한 관계 */
  --spacing-3: 0.75rem;  /* 12px */
  --spacing-4: 1rem;     /* 16px - 기본 간격 */
  --spacing-5: 1.25rem;  /* 20px */
  --spacing-6: 1.5rem;   /* 24px - 그룹 분리 */
  --spacing-8: 2rem;     /* 32px - 섹션 분리 */
  --spacing-10: 2.5rem;  /* 40px */
  --spacing-12: 3rem;    /* 48px */

  /* Border Radius (spacing과 동일 체계) */
  --radius-sm: 0.5rem;
  --radius-md: 1rem;
  --radius-lg: 1.5rem;
  --radius-xl: 2rem;
}
```

### 실전 컴포넌트 예시

#### 1. 버튼 (Optical Weight 적용)

```css
/* 기본 버튼 */
.btn {
  padding: var(--spacing-2) var(--spacing-4); /* 수직:수평 = 1:2 */
  gap: var(--spacing-2); /* 아이콘-텍스트 간격 */
  border-radius: var(--radius-sm);
}

/* 큰 버튼 */
.btn-lg {
  padding: var(--spacing-3) var(--spacing-6); /* 수직:수평 = 1:2 */
  gap: var(--spacing-2);
  border-radius: var(--radius-md);
}

/* 버튼 그룹 */
.btn-group {
  display: flex;
  gap: var(--spacing-4); /* 버튼 간 1rem */
}
```

#### 2. 리스트 (그룹핑 적용)

```css
/* 리스트 컨테이너 */
.list-container {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-4); /* 섹션 간 1rem */
}

/* 리스트 헤딩과 아이템 사이 */
.list-section {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-6); /* 헤딩-리스트 분리: 1.5rem */
}

/* 리스트 아이템 간격 */
.list-items {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-4); /* 아이템 간 1rem */
}

/* 밀접한 관계 (제목-부제) */
.list-item-header {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-2); /* 0.5rem */
}
```

#### 3. 카드 (균형잡힌 padding/gap)

```css
/* 기본 카드 */
.card {
  padding: var(--spacing-4); /* 1rem padding */
  display: flex;
  flex-direction: column;
  gap: var(--spacing-4); /* 내부 요소도 1rem gap */
  border-radius: var(--radius-md); /* padding과 동일 */
}

/* 수직 요소 많은 카드 */
.card-vertical {
  padding: var(--spacing-5); /* 1.25rem - 숨 쉴 공간 */
  display: flex;
  flex-direction: column;
  gap: var(--spacing-5);
  border-radius: var(--radius-lg);
}

/* 카드 그리드 */
.card-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: var(--spacing-4); /* 카드 간 1rem */
}
```

#### 4. 모달/다이얼로그 (섹션 분리)

```css
.modal {
  padding: var(--spacing-6); /* 외부 1.5rem */
  display: flex;
  flex-direction: column;
  gap: var(--spacing-6); /* 섹션 간 1.5rem */
  border-radius: var(--radius-lg);
}

.modal-header {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-2); /* 제목-설명: 0.5rem */
}

.modal-body {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-4); /* 옵션 간 1rem */
}

.modal-footer {
  display: flex;
  gap: var(--spacing-4);
  justify-content: space-between;
}
```

#### 5. 폼 (광학적 균형)

```css
/* 인풋 필드 */
.input {
  padding: var(--spacing-2) var(--spacing-4); /* 수직:수평 = 1:2 */
  border-radius: var(--radius-sm);
}

/* 드롭다운 */
.dropdown {
  padding: var(--spacing-2) var(--spacing-4);
  gap: var(--spacing-2); /* 텍스트-아이콘 간격 */
  border-radius: var(--radius-sm);
}

/* 폼 필드 간격 */
.form-group {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-4); /* 필드 간 1rem */
}

/* 라벨-인풋 간격 */
.form-field {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-2); /* 0.5rem */
}
```

---

## ✅ Spacing 디자인 체크리스트

### 설계 단계
- [ ] 4px/8px 기반 spacing scale 정의됨
- [ ] CSS custom properties로 spacing tokens 구현됨
- [ ] 핵심 3가지 값(0.5rem, 1rem, 1.5rem)이 80% 케이스 커버하는지 확인

### 컴포넌트 단계
- [ ] 내부 간격 < 외부 padding 원칙 준수
- [ ] 수평 흐름 요소는 수직 padding < 수평 padding (1:2~1:3)
- [ ] 버튼/카드의 padding과 border-radius 조화
- [ ] 같은 그룹 요소는 작은 간격 (0.5~1rem)
- [ ] 다른 그룹 간 큰 간격 (1.5~2rem)

### 레이아웃 단계
- [ ] UI를 논리적 그룹으로 분해
- [ ] 큰 값(1.5rem)에서 시작해서 필요시 감소
- [ ] 섹션 간 충분한 여백 (2rem 이상)
- [ ] 카드 padding = 내부 gap (시각적 균형)

### 반응형 단계
- [ ] rem 단위 사용으로 폰트 크기에 따라 간격도 스케일
- [ ] 작은 화면에서도 여유 있는 간격 유지
- [ ] 불필요하게 타이트한 간격 없음

### 일관성 체크
- [ ] 동일한 역할의 요소는 동일한 간격 사용
- [ ] spacing tokens만 사용 (임의의 값 없음)
- [ ] 디자인 시스템 문서에 spacing 규칙 명시

---

## 🎨 AlphaView 적용 포인트

### 1. AlphaView Spacing System 구축

**현재 상태 진단 필요**:
```bash
# Tailwind config 확인
frontend/nextjs-app/tailwind.config.ts

# 현재 spacing 사용 패턴 분석
grep -r "gap-\|p-\|m-\|space-" frontend/nextjs-app/src/components
```

**권장 Tailwind 확장**:
```js
// tailwind.config.ts
module.exports = {
  theme: {
    extend: {
      spacing: {
        '0': '0rem',
        '1': '0.25rem',  // 4px
        '2': '0.5rem',   // 8px - 밀접한 관계
        '3': '0.75rem',  // 12px
        '4': '1rem',     // 16px - 기본 간격
        '5': '1.25rem',  // 20px
        '6': '1.5rem',   // 24px - 그룹 분리
        '8': '2rem',     // 32px - 섹션 분리
        '10': '2.5rem',  // 40px
        '12': '3rem',    // 48px
      }
    }
  }
}
```

### 2. 우선순위 높은 개선 영역

#### A. 뉴스 카드 (LatestNewsGrid, AlphaArticleGrid)
```tsx
// 현재 (추정)
<div className="grid gap-4 p-4">  ← 카드 간격과 padding 불균형?

// 개선
<div className="grid gap-4 p-4">  ← padding = gap (균형)
  <article className="p-4 space-y-4">  ← 내부도 동일
    <header className="space-y-2">  ← 제목-메타는 밀접
```

#### B. 모달 (ArticleModal)
```tsx
// 섹션 분리 강화
<Modal>
  <div className="p-6 space-y-6">  ← 섹션 간 1.5rem
    <header className="space-y-2">  ← 제목-설명: 0.5rem
    <section className="space-y-4"> ← 본문: 1rem
    <footer className="flex gap-4">  ← 버튼: 1rem
```

#### C. 필터/액션 버튼 (AlphaFilterBar)
```tsx
// 광학적 무게 적용
<button className="px-4 py-2 gap-2">  ← 수직:수평 = 1:2
  <Icon /> <span>Filter</span>
</button>

// 버튼 그룹
<div className="flex gap-4">  ← 버튼 간 1rem
```

#### D. Admin 페이지 (stats, crawler)
```tsx
// 통계 카드 그리드
<div className="grid grid-cols-3 gap-4">  ← 카드 간 1rem
  <Card className="p-4 space-y-4">  ← padding = gap
```

### 3. 구체적 개선 작업

| 파일 | 현재 문제 (추정) | 개선 방향 |
|------|------------------|-----------|
| `LatestNewsGrid.tsx` | 카드 간격 불균형 | padding = gap 일치 |
| `ArticleModal.tsx` | 섹션 분리 불명확 | space-y-6으로 그룹 분리 |
| `AlphaFilterBar.tsx` | 버튼 padding 불균형 | px-4 py-2 (1:2 비율) |
| `CompanyInsightsSection.tsx` | 탭 간격 | space-x-4 적용 |
| `Header.tsx` | 네비게이션 간격 | gap-6 (섹션 분리) |

### 4. 빠른 감사(Audit) 명령어

```bash
# 일관성 없는 spacing 값 찾기 (Tailwind 클래스)
grep -rP "(gap|space|p|m)-(1|3|5|7|9|11)" frontend/nextjs-app/src/components

# 인라인 스타일의 픽셀 값 찾기
grep -rP "padding.*px|margin.*px|gap.*px" frontend/nextjs-app/src/components

# 불균형한 padding (p-x와 p-y가 섞임)
grep -rP "p-\d+.*px-\d+|px-\d+.*p-\d+" frontend/nextjs-app/src/components
```

### 5. 단계별 실행 계획

**Phase 1: 토큰 정의** (1일)
- [ ] `tailwind.config.ts`에 spacing scale 추가
- [ ] CSS custom properties 정의 (필요시)

**Phase 2: 핵심 컴포넌트** (2~3일)
- [ ] 버튼 컴포넌트 (px-4 py-2 표준화)
- [ ] 카드 컴포넌트 (padding = gap)
- [ ] 모달 (섹션 간 space-y-6)

**Phase 3: 페이지 레벨** (3~5일)
- [ ] 메인 페이지 (섹션 간 space-y-8)
- [ ] 뉴스 그리드 (카드 간격 통일)
- [ ] Admin 페이지 (통계 카드 균형)

**Phase 4: 감사 및 문서화** (1일)
- [ ] 전체 spacing 일관성 체크
- [ ] 디자인 시스템 문서 업데이트

---

## 📚 핵심 요약

### 꼭 기억할 3가지

1. **내부 < 외부**: 내부 간격은 항상 외부 padding보다 작거나 같아야 함
2. **큰 값에서 시작**: 1.5rem에서 시작해서 줄이기 (0.5rem부터 늘리지 말 것)
3. **일관성이 왕**: 잘못된 값이라도 일관되면 "괜찮아 보임"

### 80% 케이스를 커버하는 핵심 값

| 값 | 용도 |
|----|------|
| **0.5rem** | 밀접한 관계 (제목-부제, 아이콘-텍스트) |
| **1rem** | 기본 간격 (버튼 그룹, 리스트, padding) |
| **1.5rem** | 그룹 분리 (섹션 간) |

### 실수하지 않으려면

- ❌ 임의의 픽셀 값 사용 (17px, 23px 등)
- ❌ 내부 간격 > 외부 padding
- ❌ 수평 흐름 요소에 동일한 수직/수평 padding
- ❌ 0.5rem부터 시작해서 늘리기
- ✅ spacing scale만 사용
- ✅ 큰 값에서 시작
- ✅ 그룹핑 먼저, 간격은 나중에

---

**다음 단계**: AlphaView 프론트엔드에서 `tailwind.config.ts` 점검 및 spacing scale 적용 시작
