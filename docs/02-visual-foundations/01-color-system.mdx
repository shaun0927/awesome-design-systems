---
title: "디자인 시스템의 컬러: 16가지 핵심 팁"
sidebar_label: "컬러 시스템"
sidebar_position: 1
audience: both
tags: [color, palette, accessibility, oklch]
---

> Original issue: shaun0927/stocktitan-crawler#555

import DevQuickStart from '@site/src/components/DevQuickStart';

<DevQuickStart
  what="A systematic color architecture (palette, tints, feedback, contrast) replaces ad-hoc color picking with scalable, accessible tokens"
  learn="16 practical tips for building color systems covering primary palettes, tint/shade scales, feedback colors, and WCAG contrast"
  able="Define a color system with CSS custom properties, contrast-checking utilities, and modern color formats (oklch)"
/>

## 핵심 개념

컬러는 단순히 "예쁜 색 고르기"가 아니라, **확장 가능하고 접근 가능하며 일관된 시스템을 구축하는 과정**

#### 4가지 영역
1. **Primary Palette**: 브랜드 컬러, 중립색, 인터랙티브 블루
2. **Tints & Shades**: 각 컬러의 밝기 범위 설정
3. **Secondary Palettes**: 피드백 컬러, 테마 컬러
4. **Contrast & Accessibility**: WCAG 2.0 준수

## 🎯 16가지 컬러 팁

### Primary Palette

#### #1. 브랜드 컬러를 빠르게 확정하라
- 핵심 브랜드 컬러 1~3개를 초기에 결정
- 변경 시 시스템 전체에 파급 효과

#### #2. 브랜드 컬러 변경 시 브랜드팀 참여시켜라
- 브랜드 컬러 = 브랜드팀 영역
- 조정 전 반드시 협의

#### #3. 중간 중립색을 버려라

```css
:root {
  /* 밝은 그레이 - 안전 */
  --color-gray-50: oklch(97% 0 0);
  --color-gray-100: oklch(93% 0 0);
  --color-gray-200: oklch(87% 0 0);
  --color-gray-300: oklch(80% 0 0);

  /* 중간 그레이 (40~60) - 접근성 위험 지대, 최소한으로 */

  /* 어두운 그레이 - 안전 */
  --color-gray-700: oklch(37% 0 0);
  --color-gray-800: oklch(27% 0 0);
  --color-gray-900: oklch(18% 0 0);
}
```

#### #4. "디지털 블루"를 채택하라
- 링크는 태초부터 파란색
- 채도 높은 파란색을 유틸리티 컬러로

### Tints & Shades

#### #5. 명암 단계를 스택으로 시각화하라

```css
:root {
  --color-indigo-50:  oklch(95% 0.03 280);
  --color-indigo-100: oklch(88% 0.06 280);
  --color-indigo-200: oklch(78% 0.09 280);
  --color-indigo-500: oklch(50% 0.18 280); /* Base */
  --color-indigo-900: oklch(25% 0.12 280);
}
```

#### #6. Lightness 기준으로 명명하라

```css
/* CSS custom properties with numeric scale */
--color-gray-05: oklch(97% 0 0);  /* Lightness 기반 */
--color-gray-10: oklch(93% 0 0);
--color-gray-15: oklch(90% 0 0);  /* 중간값 추가 용이 */
--color-gray-20: oklch(87% 0 0);
```

#### #7. 명암 단계 수량을 제한하라
- 대부분 시스템: 4~6단계면 충분
- Material Design(10단계)은 무수한 제품용

#### #8. 색상 변형 방법을 명시하라
- **Handpicked**: 디자이너가 직접 선택
- **Functional Transform**: `darken(10%)` - 신중히 사용

### Secondary Palettes

#### #9. 피드백 컬러 세트를 정의하라

```css
:root {
  --color-success: oklch(65% 0.18 145);     /* Green */
  --color-success-bg: oklch(95% 0.04 145);
  --color-warning: oklch(75% 0.15 85);      /* Yellow */
  --color-warning-bg: oklch(96% 0.04 85);
  --color-error: oklch(55% 0.22 25);        /* Red */
  --color-error-bg: oklch(95% 0.04 25);
  --color-info: oklch(60% 0.15 250);        /* Blue */
  --color-info-bg: oklch(95% 0.03 250);
}
```

#### #10. 테마 다양성을 보여라
- Marriott 예시: Courtyard, Renaissance, JW Marriott 등

#### #11. 테마 작동 방식을 정의하라
```
테마 적용 가능: 버튼 background, 활성 탭 background
테마 적용 금지: 긴 텍스트, 링크 색상 (가독성 문제)
```

#### #12. 컬러 믹싱 가이드는 나중에 (최소한)
- 대부분 시스템 = 그런 유연성 불필요
- 선택을 전파하는 도구 제공 (선택 자체는 사용자)

### Contrast & Accessibility

#### #13. 대비를 조기에 반복적으로 점검하라
- WCAG 2.0 규칙 숙지 (3.0 / 4.5)
- Tanaguru 같은 도구 상시 활용

#### #14. 접근 가능한 색상 선택 범위를 탐색하라
```
대화 변화:
전: "실패했네, 다시 시도"
후: "블루가 이 정도로 어두워야 하는구나"
```

#### #15. 반전 조합 해결하라
- Light on Dark / Dark on Light
- SASS 함수로 대비 매트릭스 자동 계산

#### #16. 컬러로 접근성 인식을 확산하라
- 컬러 = 시스템의 핵심
- 접근성 대비 = 컬러의 핵심
- 브랜드 매니저, 디자인 리드, 개발자, 임원 모두 주목

## 📊 실무 적용 체크리스트

### 프로젝트 초기 (Week 1-2)
- [ ] 브랜드 컬러 1~3개 확정 (브랜드팀 협의)
- [ ] 디지털 블루 선택 (접근성 확인)
- [ ] 중립색 팔레트 설정 (중간 그레이 제외)
- [ ] 피드백 컬러 4종 정의

### 접근성 검증 (Week 7-8)
- [ ] Tanaguru로 모든 조합 점검
- [ ] 대비 매트릭스 작성
- [ ] 반전 조합 검토
- [ ] 범위 시각화 문서 추가

## 대비 체크 유틸리티

```javascript title="contrast-checker.js"
/**
 * WCAG 2.0 contrast ratio calculator
 * Returns ratio like 4.5, 7.0, etc.
 */
function getContrastRatio(rgb1, rgb2) {
  const luminance = (rgb) => {
    const [r, g, b] = rgb.map((c) => {
      c = c / 255;
      return c <= 0.03928 ? c / 12.92 : Math.pow((c + 0.055) / 1.055, 2.4);
    });
    return 0.2126 * r + 0.7152 * g + 0.0722 * b;
  };

  const l1 = luminance(rgb1);
  const l2 = luminance(rgb2);
  const lighter = Math.max(l1, l2);
  const darker = Math.min(l1, l2);

  return (lighter + 0.05) / (darker + 0.05);
}

// Usage
const ratio = getContrastRatio([255, 255, 255], [51, 65, 85]);
console.log(`Contrast ratio: ${ratio.toFixed(2)}:1`);
// AA normal text requires >= 4.5:1
// AA large text requires >= 3.0:1
```

## Modern CSS: color-mix() 활용

```css
:root {
  --color-primary: oklch(55% 0.2 250);

  /* color-mix()로 hover/active 자동 생성 */
  --color-primary-hover: color-mix(
    in oklch, var(--color-primary), black 15%
  );
  --color-primary-active: color-mix(
    in oklch, var(--color-primary), black 25%
  );
  --color-primary-subtle: color-mix(
    in oklch, var(--color-primary), white 85%
  );
}
```

---
*출처: Nathan Curtis (EightShapes)*

---

## 📎 Related Articles

import CrossRef from '@site/src/components/CrossRef';

<CrossRef
  related={[
    { path: "/docs/design-tokens/token-tips", label: "디자인 토큰 10가지 핵심 팁" },
    { path: "/docs/visual-foundations/ui-colors", label: "UI 색상 선택 노하우" },
    { path: "/docs/design-tokens/token-taxonomy", label: "Token Taxonomy - Audit to Implementation" },
  ]}
/>
