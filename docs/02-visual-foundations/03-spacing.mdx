---
title: "Space in Design Systems: 6가지 핵심 개념"
sidebar_label: "스페이싱 시스템"
sidebar_position: 3
audience: developer
tags: [spacing, layout, components, gap, css]
---

import DevQuickStart from '@site/src/components/DevQuickStart';

<DevQuickStart
  what="Space tokens (inset, stack, inline) replace arbitrary pixel values with a consistent, reusable spacing vocabulary"
  learn="6 spacing concepts (Inset, Squish, Stretch, Stack, Inline, Grid) and how to implement them with CSS custom properties and gap"
  able="Build a complete spacing token system using CSS custom properties with modern gap-based layouts"
/>

## 핵심 개념

#### Space의 중요성
- **CSS 속성 사용 빈도**: Space 관련 속성이 Color 다음으로 가장 많이 사용됨
- **소통의 장벽**: 디자이너-개발자 간 가장 큰 갈등 요소
- **Red-line Specs의 폐해**: 픽셀 단위 수작업 표시는 비효율적

### Space 기본 원칙

#### 1. Grid ≠ Space
- **Grid는 Space를 사용하는 컴포넌트일 뿐**
- Grid의 margin, gutter 값을 더 깊은 공간 개념과 정렬할 것

#### 2. Base Number는 16
```
Base 16을 권장하는 이유:
- 기본 폰트 사이즈로 적합
- 모든 화면 해상도(320, 768, 1024)의 약수
- 배수(32, 64) 및 약수(8, 4, 2)가 기억하기 쉬움
```

#### 3. 비선형 스케일 사용
```scss
// 기하급수적 진행 (Geometric Progression)
$space-xxs: 2px;   // base / 8
$space-xs:  4px;   // base / 4
$space-s:   8px;   // base / 2 (25% 사용)
$space-m:   16px;  // base (41% 사용)
$space-l:   32px;  // base * 2 (21% 사용)
$space-xl:  64px;  // base * 4 (4% 사용)
```

#### 4. T-shirt 사이즈로 네이밍
```
XXS, XS, S, M (기준점), L, XL, XXL
```

## 🎯 6가지 Space 개념

### Concept 1: Inset (정사각형 인셋)
```
┌──────────────────┐
│  ←  padding  →   │
│  ┌────────────┐  │
│ ↑│  Content   │↑ │
│ ↓│            │↓ │
│  └────────────┘  │
└──────────────────┘
```
- **정의**: 4방향 모두 동일한 padding
- **사용 사례**: Card, Block message, Footer
- **반응형**: Mobile First → 768px+ 에서 Large로 확장

### Concept 2: Squish Inset (압축된 인셋)
```
┌──────────────────┐
│ ←     wide    → │
│ ┌────────────┐  │
↕ │  Content   │  │ ← 세로 50% 감소
│ └────────────┘  │
└──────────────────┘
```
- **정의**: 상하 padding을 50% 줄임
- **사용 사례**: Button, Data table cells, List items

### Concept 3: Stretch Inset (늘어난 인셋)
```
┌──────────────────┐
│ ← narrow →       │
│ ┌────────────┐   │
↕ │            │   │ ← 세로 증가
↕ │  Content   │   │
│ └────────────┘   │
└──────────────────┘
```
- **정의**: 세로 padding 증가
- **사용 사례**: Textbox, Textarea, Form elements

### Concept 4: Stack (수직 스택)
```
┌────────────────┐
│  Element 1     │
├────────────────┤ ← margin-bottom
│  Element 2     │
├────────────────┤ ← margin-bottom
│  Element 3     │
└────────────────┘
```
- **정의**: 세로로 쌓을 때 사용하는 margin
- **압도적 사용 빈도**: 대부분의 UI가 세로 스크롤
- **사용 사례**: Card 내부 섹션, Form 필드, Module 간 간격

### Concept 5: Inline (인라인 배치)
```
[Tag 1] [Tag 2] [Tag 3]
[Tag 4] [Tag 5]
```
- **정의**: 좌우로 흐르며 자동 줄바꿈되는 요소 간 간격
- **사용 사례**: Pills, Tags, Breadcrumbs, Footer links

### Concept 6: Grid (그리드)
- **정의**: Grid의 margin과 gutter를 전체 공간 시스템과 정렬
- **타이밍**: 공간 시스템이 안정화된 후 Grid를 재검토

## 💡 실무 구현 예시

### Phase 1: Foundation (CSS Custom Properties)

```css
:root {
  --space-base: 1rem;    /* 16px */

  --space-xxs: 0.125rem; /* 2px */
  --space-xs:  0.25rem;  /* 4px */
  --space-s:   0.5rem;   /* 8px */
  --space-m:   1rem;     /* 16px */
  --space-l:   2rem;     /* 32px */
  --space-xl:  4rem;     /* 64px */
}
```

### Phase 2: Concepts

```css
/* Inset (정사각형 padding) */
.inset-m { padding: var(--space-m); }

/* Squish Inset (세로 50% 감소) */
.inset-squish-m { padding: var(--space-s) var(--space-m); }

/* Stack (수직 간격 - modern: use gap) */
.stack-m { display: flex; flex-direction: column; gap: var(--space-m); }

/* Inline (수평 간격 - modern: use gap) */
.inline-s { display: flex; flex-wrap: wrap; gap: var(--space-s); }
```

### Phase 3: Application (Modern CSS with gap)

```css
.card {
  padding: var(--space-m);  /* Inset M: 16px 전방향 */
  display: flex;
  flex-direction: column;
  gap: var(--space-m);      /* Stack M: 자식 요소 간 16px */
}

.card__button {
  padding: var(--space-s) var(--space-m); /* Squish M: 8px 16px */
}

.card__tags {
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-s);      /* Inline S: 태그 간 8px */
}
```

## 🎨 Card 컴포넌트 실전 분석

```
Card Component 구조:
┌─────────────────────────────────┐
│ 1. .card (container)            │ ← Inset M
│   ┌─────────────────────────┐   │
│   │ 2. Type label           │   │ ← Stack S
│   │ 3. Title                │   │ ← Stack M
│   │ 4. Description          │   │ ← Stack M
│   │ 5. [Pills]              │   │ ← Inset S
│   │   [Pill] [Pill]         │   │ ← Inline S
│   │ 6. [Button]             │   │ ← Inset Squish M
│   └─────────────────────────┘   │
└─────────────────────────────────┘
```

## 📊 실무 적용 효과

### Before (시스템 없이)
```css
.card { padding: 22px; margin-bottom: 18px; }
.title { margin-bottom: 14px; }
```
**문제**: 재사용 불가, 소통 비용 높음, QA 논쟁

### After (시스템 적용)
```css
.card {
  padding: var(--space-m);           /* 16px */
  display: flex;
  flex-direction: column;
  gap: var(--space-m);               /* 16px between children */
}
```
**효과**:
- 패턴 명확 → 재사용 ↑
- 디자이너도 이해 가능 → 소통 비용 ↓
- QA 간소화 → "시스템 규칙대로만 확인"

## 고급 활용: Density Control

```css
/* Density via CSS custom properties override */
:root {
  --space-density-factor: 1;
}

[data-density="compact"] {
  --space-density-factor: 0.75;  /* 25% 감소 */
}

[data-density="comfortable"] {
  --space-density-factor: 1.25;  /* 25% 증가 */
}

.card {
  padding: calc(var(--space-m) * var(--space-density-factor));
  gap: calc(var(--space-s) * var(--space-density-factor));
}
```

## 🎓 핵심 Takeaway

1. **시각적으로 가르쳐라**: Cheatsheet 제공
2. **Generic Helper도 제공하되 모니터링**
3. **변수명에 Padding/Margin 넣지 마라**: 개념 기반 네이밍
4. **Line Height 충돌을 시스템적으로 해결**
5. **Density를 Space 개념으로 조절**

## 참고 자료

- Nathan Curtis, "Space in Design Systems" (EightShapes, 2016)
- [Gestalt Principles of Proximity](https://www.interaction-design.org/literature/topics/gestalt-principles) — 근접성의 법칙
- [Every Layout](https://every-layout.dev/) — 현대 CSS 레이아웃 패턴
- [Spacing Systems by Dan Eden](https://daneden.me/blog/2018/spacing-systems) — Dropbox 간격 시스템 사례
- [Primer Design System - Spacing](https://primer.style/foundations/spacing) — GitHub의 간격 시스템
- Brad Frost, "Atomic Design" (2016) — 컴포넌트 간격 구조화

> **실제 사례**: Shopify Polaris는 Inset/Stack/Inline 개념을 컴포넌트 API로 구현하여, `<Stack gap="tight">` 또는 `<Inline gap="loose">`처럼 선언적으로 간격을 제어합니다. 이를 통해 디자이너가 픽셀값을 지정하지 않고도 일관된 간격을 적용할 수 있습니다.

> **심화 이론**: Squish/Stretch Inset은 **광학적 보정(Optical Correction)** 원리에 기반합니다. 인간의 시각은 수평 방향보다 수직 방향의 간격을 더 좁게 인식하므로, 같은 padding을 적용하면 수직이 답답해 보입니다. Button의 padding을 `8px 16px`(1:2 비율)로 설정하는 것은 시각적 균형을 맞추는 광학적 보정입니다.

> **심화 이론**: CSS `gap` 속성은 **Flexbox/Grid의 패러다임 전환**을 상징합니다. 과거에는 `margin-bottom`으로 간격을 만들었지만, 마지막 요소의 불필요한 마진을 제거하는 `:last-child` 선택자가 필요했습니다. `gap`은 이 문제를 근본적으로 해결하며, 더 선언적이고 유지보수가 쉬운 코드를 가능하게 합니다.

---
*출처: Nathan Curtis (EightShapes, 2016)*

---

## 📎 Related Articles

import CrossRef from '@site/src/components/CrossRef';

<CrossRef
  related={[
    { path: "/docs/visual-foundations/perfect-spacing", label: "완벽한 간격 선택 노하우" },
    { path: "/docs/visual-foundations/sizing", label: "사이즈 시스템 - 컴포넌트 높이 통일" },
    { path: "/docs/design-tokens/scalable-scales", label: "확장 가능한 스케일 시스템" },
  ]}
/>
