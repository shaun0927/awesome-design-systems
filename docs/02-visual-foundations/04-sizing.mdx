---
title: "Size in Design Systems: 컴포넌트 높이 통일"
sidebar_label: "사이즈 시스템"
sidebar_position: 4
tags: [sizing, components, density]
---

> Original issue: shaun0927/stocktitan-crawler#566

## Size in Design Systems: 컴포넌트 높이 통일 (Nathan Curtis, 2019)

### 📌 핵심 개념

#### 사이즈의 정의
```
사이즈 = Typography + Padding + Border
       ↓
     Height (컴포넌트 높이)
```

**목표**: 모든 컴포넌트가 **동일한 높이(Height)**를 공유하도록 통일

#### 밀도(Density)와의 관계
- **고밀도**: 데이터 중심 앱, 스크롤 최소화
- **저밀도**: 마케팅 사이트, CTA 집중

## 🎯 단계별 구축 가이드

### Step 1: 사이즈 레벨 정의
```
Small / Medium / Large (3단계) ← 가장 일반적

Small:  고밀도 데이터 인터페이스
Medium: 기본값, 범용
Large:  마케팅/온보딩 플로우
```

### Step 2: Button/Input부터 시작
```scss
Small Button:
  - font-size: 14px
  - padding: 6px 12px
  - height: 28px (자동 계산)

Medium Button:
  - font-size: 16px
  - padding: 10px 16px
  - height: 37px

Large Button:
  - font-size: 18px
  - padding: 14px 20px
  - height: 46px
```

### Step 3: Typography 모델 통일
```scss
@mixin text-small() {
  font-size: 14px;
  line-height: 1.5;
}

@mixin text-medium() {
  font-size: 16px;
  line-height: 1.5;
}
```

### Step 4: Space 모델 통일
```scss
$inset-squish-half-x: 6px 12px;   // Small
$inset-squish-3q-x: 10px 16px;    // Medium
$inset-squish-1-x: 14px 20px;     // Large
```

### Step 5: 컴포넌트별 미세 조정
```scss
// Select는 border 1px 때문에 padding 1px 줄임
.select--small {
  @include text-small();
  padding: 5px 12px; // 6px - 1px
  border: 1px solid;
}
```

## 📊 사이즈별 컴포넌트 높이 통일

| 사이즈 | Button | Input | Select | List Item | **높이** |
|--------|--------|-------|--------|-----------|---------|
| Small  | 14px font | 14px font | 14px font | 14px font | **28px** |
| Medium | 16px font | 16px font | 16px font | 16px font | **37px** |
| Large  | 18px font | 18px font | 18px font | 18px font | **46px** |

**핵심**: 같은 사이즈 레벨에서 **모든 컴포넌트가 동일한 높이**

## 💡 실무 노하우

### 1. 모든 컴포넌트에 모든 사이즈가 필요하지 않음

| 컴포넌트 | Small | Medium | Large | 이유 |
|---------|-------|--------|-------|------|
| Alert | ❌ | ✅ | ✅ | Small은 주목도 떨어짐 |
| Tag | ✅ | ✅ | ❌ | Large는 지나치게 눈에 띔 |
| Button | ✅ | ✅ | ✅ | 모든 사이즈 필요 |

### 2. Width는 별도 네이밍

```scss
// ❌ 나쁜 예: S/M/L을 Width에 사용
.modal--small { width: 400px; }

// ✅ 좋은 예: Width는 명시적 값 기반
.modal--width-400 { width: 400px; }
.modal--narrow { width: 400px; }
```

### 3. 점진적 마이그레이션 전략

```
1단계: 모든 컴포넌트의 현재 사이즈 측정 (Audit)
2단계: 목표 높이 설정 (28px / 37px / 46px)
3단계: 분기별/반기별 대규모 업데이트
4단계: Major Version Bump (Breaking Change)
```

## 🚨 흔한 실수와 해결

### ❌ 실수 1: 높이를 직접 지정
```scss
// 나쁜 예
.button--small {
  height: 28px; // 고정 높이
  overflow: hidden;
}
```
**문제**: 텍스트가 2줄로 늘어나면 잘림

**해결**:
```scss
// 좋은 예
.button--small {
  font-size: 14px;
  line-height: 1.5;
  padding: 6px 12px;
  // height는 자동 계산
}
```

### ❌ 실수 2: 컴포넌트별 증분 구현
**문제**: Button, Input, Select를 각각 다른 기준으로 구현

**해결**: Mixin/Token으로 **사전 정의** 후 일괄 적용

## 💡 검증 체크리스트

### 설계 단계
- [ ] 사이즈 레벨 정의 (2~3단계)
- [ ] 목표 높이 설정 (Small: ~28px, Medium: ~36-40px, Large: ~44-48px)
- [ ] 핵심 컴포넌트 선정 (Button, Input 우선)

### 구현 단계
- [ ] Typography Mixin/Token 생성
- [ ] Space Token 정의
- [ ] 컴포넌트별 오버라이드 (border/icon 고려)

### 검증 단계
- [ ] 높이 측정
- [ ] 시각적 정렬 확인
- [ ] 크로스 브라우저 테스트

## 🎯 핵심 원칙

1. **높이 통일**: 같은 사이즈 레벨의 모든 컴포넌트는 동일한 높이
2. **Typography + Space**: 높이를 직접 지정하지 말고 자연스럽게 계산
3. **2~3단계 사이즈**: 과도한 세분화 지양
4. **Mixin/Token 우선**: 개별 컴포넌트 전에 공통 툴링 구축
5. **선택적 적용**: 모든 컴포넌트가 모든 사이즈를 가질 필요 없음

---
*출처: Nathan Curtis (EightShapes, 2019)*

---

## 📎 Related Articles

import CrossRef from '@site/src/components/CrossRef';

<CrossRef
  related={[
    { path: "/docs/visual-foundations/spacing", label: "스페이싱 시스템" },
    { path: "/docs/component-design/buttons", label: "버튼 컴포넌트 12가지 팁" },
    { path: "/docs/component-design/right-sizing", label: "Right-Sizing 컴포넌트 설계" },
  ]}
/>
