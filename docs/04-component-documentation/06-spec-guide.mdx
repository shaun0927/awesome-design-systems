---
title: "컴포넌트 명세서 작성 가이드"
sidebar_label: "명세서 작성 가이드"
sidebar_position: 6
tags: [specs, handoff, documentation, accessibility, figma]
---

> Original issue: shaun0927/stocktitan-crawler#558

## 핵심 개념

### 1. 스펙 문서의 필요성 변화
**현재 (2023~)**:
- 멀티플랫폼 빌드 (iOS, Android, Web 등)
- 디자이너 1명 → 여러 개발팀에 전달
- 접근성(Accessibility) 중요성 상승
- 컴포넌트 복잡도 증가
- **명시적 문서화 필수**

### 2. 핵심 원칙
> **"디자인 의도 이해가 보물찾기가 되어서는 안 된다"**

**Figma Inspect의 한계**:
- 시각적 속성만 확인 가능
- 동작(Behavior), 접근성, 코드 전용 속성 누락

**스펙의 역할**:
- 디자이너가 이미 알고 있는 모든 디자인 결정을 명시적으로 전달
- 플랫폼 간 일관성 보장
- 버전 간 변경사항 추적

## 실무 노하우

### 1. 스펙 파일 구성 전략
```
Specifications
  Cover
  Component status
  About this file
  ━━━ Foundations ━━━
  Color, Layout and spacing, Shape, Typography
  ━━━ Components ━━━
  Accordion, Alert, Badge, ...
```

### 2. 스펙 작성 우선순위

**필수 (모든 컴포넌트)**:
- Anatomy, Properties, Layout and spacing

**상황별 필수**:
- Behavior (인터랙션이 있는 컴포넌트)
- Accessibility (모든 컴포넌트, 특히 form 요소)
- Motion (애니메이션이 있는 컴포넌트)
- Component tokens (테마가 있는 시스템)

### 3. 디자인 가이드라인 vs 스펙

| | 스펙 (Specs) | 디자인 가이드라인 |
|---|---|---|
| **목적** | How to MAKE (만드는 방법) | How to USE (사용 방법) |
| **독자** | 개발자 (1차), 디자이너 (2차) | 디자이너, 프로덕트 팀 |
| **내용** | 구현 상세 (토큰, props, 접근성, 동작) | 사용 사례, Do/Don't, 베스트 프랙티스 |
| **시점** | 컴포넌트 디자인 안정화 시점 | 릴리스 전후 |

## 스펙 문서 작성법

### Step 1: Anatomy 작성
```
① {Layer name}
   ◇ Depends on: {Component name}
   - prop1: value
   - prop2: value

   Ⓣ {Text element}
   Fill style: {Token/Style name}
   Text style: {Typography token}
   Text align: {Alignment}
```

### Step 2: Properties 작성
```
[Property name]

[Option 1] | [Option 2] | [Option 3]
Default    | Alternative | Alternative

Property | Type    | Default | Options
---------|---------|---------|------------------
size     | enum    | medium  | small, medium, large
required | boolean | false   | true, false
```

### Step 3: Layout and Spacing 작성
```
◇ {Layer name}
Direction: Horizontal / Vertical
Alignment: Top left / Center / ...
Item spacing: {value}
Padding top/bottom/left/right: {value}
```

### Step 4: Behavior 작성
```
Event: click / mousedown / drag / ...

Trigger: {event}
Result: {state change description}

[Before state] → [After state]
```

### Step 5: Accessibility 작성

**Screen Reading**:
```
◇ {Component name}

Reading: {format with placeholders}

Example: "{actual example sentence}"

Role: {ARIA role}
```

**Focus Order**:
```
1. {Name} (Role)
2. {Name} (Role)
...
```

### Step 6: Component Tokens 작성
```
Token | Alias       | Figma style    | Value
------|-------------|----------------|-------
⬛ 00 | $esds-...   | (Figma style)  | (Value)
      | (Description)|                |
```

### Step 7: Version History 작성
```
Version #.#.# • MMM DD, YYYY

New features
• {Description}

Enhancements
• {Description}

Fixes
• {Description}
```

## 작성 체크리스트

### Anatomy
- [ ] 모든 요소에 번호가 매겨졌는가?
- [ ] 중첩 컴포넌트 의존성이 명시되었는가?
- [ ] 텍스트 스타일과 Fill 스타일이 기록되었는가?
- [ ] 레이어 이름이 코드에서 사용할 이름과 일치하는가?

### Properties
- [ ] 모든 props가 나열되었는가?
- [ ] 기본값이 명시되었는가?
- [ ] 옵션 값이 열거되었는가?

### Accessibility
- [ ] Screen reading 형식이 정의되었는가?
- [ ] ARIA role이 명시되었는가?
- [ ] Focus order가 정의되었는가?

## 주요 인사이트

1. **"보물찾기"를 없애라**: Figma Inspect는 보조 도구, 유일한 소스가 아님
2. **스펙 ≠ 가이드라인**: 스펙을 먼저, 가이드라인은 릴리스 전에
3. **멀티플랫폼 시대의 필수품**: 한 디자인 → 여러 플랫폼
4. **접근성은 선택이 아닌 필수**: 디자인 단계부터 고려
5. **네이밍의 결정적 순간**: 스펙 핸드오프 = 네이밍 합의의 시점

---
*출처: Nathan Curtis (EightShapes)*
*분석일: 2026-02-06*

---

## 📎 Related Articles

import CrossRef from '@site/src/components/CrossRef';

<CrossRef
  related={[
    { path: "/docs/component-documentation/design-guidelines", label: "Component Design Guidelines" },
    { path: "/docs/component-design/states", label: "States Management - 상태 관리의 정석" },
    { path: "/docs/accessibility/accessible-systems", label: "접근성 기초" },
  ]}
/>
