---
title: "스마트한 웹사이트 구축 방법론 정리 - The Smart Way to Build Websites"
sidebar_label: "스마트 구축법"
sidebar_position: 2
audience: developer
tags: ["components", "reusability", "design-first", "workflow"]
---

import DevQuickStart from '@site/src/components/DevQuickStart';
import CrossRef from '@site/src/components/CrossRef';

<DevQuickStart
  what="스마트 웹사이트는 CSS Grid와 Container Queries로 구현됩니다"
  learn="CSS Grid, Container Queries, 적응형 컴포넌트"
  able="CSS Grid와 Container Queries로 적응형 레이아웃을 구축할 수 있습니다"
/>

## 핵심 철학

### 컴포넌트 기반 사고방식

웹사이트는 반복 가능한 컴포넌트의 조합입니다. 매번 새로 만들지 말고, 재사용 가능한 블록으로 구성합니다.

```tsx
// Bad: 섹션마다 새로운 컴포넌트
<FeatureSection1 />
<FeatureSection2 />
<FeatureSection3 />

// Good: 하나의 컴포넌트 + props
<FeatureSection icon="🚀" title="Fast" details="Lightning speed" />
<FeatureSection icon="🔒" title="Secure" details="Bank-level encryption" />
<FeatureSection icon="📱" title="Mobile" details="Works everywhere" />
```

### Design First, Then Build

코드 작성 전 반드시 디자인 단계를 거칩니다.

**프로세스:**
1. 영감 수집 (Mobbin, 경쟁사 분석)
2. 패턴 분석 (반복되는 컴포넌트 식별)
3. 컴포넌트 설계 (props 정의)
4. 구현

---

## CSS Grid로 적응형 레이아웃 구현

### 기본 Grid 시스템

```css
.layout {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(min(300px, 100%), 1fr));
  gap: 2rem;
}
```

**작동 원리:**
- `auto-fit`: 가능한 많은 열을 생성하고 빈 열 제거
- `minmax(min(300px, 100%), 1fr)`: 최소 300px, 모바일에서 오버플로 방지
- `gap: 2rem`: 자식 간 간격

### 복잡한 레이아웃 템플릿

```css
.dashboard {
  display: grid;
  grid-template-areas:
    "header header header"
    "sidebar main main"
    "footer footer footer";
  grid-template-columns: 250px 1fr 1fr;
  gap: 1rem;
}

.header { grid-area: header; }
.sidebar { grid-area: sidebar; }
.main { grid-area: main; }
.footer { grid-area: footer; }

@media (max-width: 768px) {
  .dashboard {
    grid-template-areas:
      "header"
      "main"
      "sidebar"
      "footer";
    grid-template-columns: 1fr;
  }
}
```

---

## Container Queries로 컴포넌트 적응

Container Queries는 부모 컨테이너 크기에 따라 스타일을 변경합니다.

### 기본 사용법

```css
.card-container {
  container-type: inline-size;
  container-name: card;
}

.card-title {
  font-size: 1rem;
}

@container card (min-width: 400px) {
  .card-title {
    font-size: 1.5rem;
  }

  .card-layout {
    display: flex;
    gap: 1rem;
  }
}
```

### React 적응형 카드 컴포넌트

```tsx
interface CardProps {
  icon: string;
  title: string;
  description: string;
}

export function AdaptiveCard({ icon, title, description }: CardProps) {
  return (
    <div className="card-container">
      <div className="card-layout">
        <span className="card-icon">{icon}</span>
        <div>
          <h3 className="card-title">{title}</h3>
          <p className="card-description">{description}</p>
        </div>
      </div>
    </div>
  );
}
```

```css
.card-container {
  container-type: inline-size;
  padding: 1rem;
  border: 1px solid #e0e0e0;
  border-radius: 8px;
}

/* 좁은 컨테이너: 세로 레이아웃 */
.card-layout {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}

.card-icon {
  font-size: 2rem;
}

/* 넓은 컨테이너 (400px+): 가로 레이아웃 */
@container (min-width: 400px) {
  .card-layout {
    flex-direction: row;
    align-items: center;
  }

  .card-icon {
    font-size: 3rem;
  }

  .card-title {
    font-size: 1.5rem;
  }
}
```

---

## 재사용 가능한 섹션 컴포넌트

### 2-Column 레이아웃 컴포넌트

```tsx
interface TwoColumnProps {
  imageSrc: string;
  imageAlt: string;
  subheading: string;
  heading: string;
  details: string;
  reverse?: boolean;
}

export function TwoColumnSection({
  imageSrc,
  imageAlt,
  subheading,
  heading,
  details,
  reverse = false,
}: TwoColumnProps) {
  return (
    <section className={`two-column ${reverse ? 'reverse' : ''}`}>
      <div className="image-container">
        <img src={imageSrc} alt={imageAlt} />
      </div>
      <div className="content">
        <p className="subheading">{subheading}</p>
        <h2>{heading}</h2>
        <p className="details">{details}</p>
      </div>
    </section>
  );
}
```

```css
.two-column {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(min(400px, 100%), 1fr));
  gap: 2rem;
  align-items: center;
}

.two-column.reverse {
  direction: rtl;
}

.two-column.reverse > * {
  direction: ltr;
}

.image-container img {
  width: 100%;
  height: auto;
  border-radius: 8px;
}
```

### Bento Grid 레이아웃

```tsx
interface BentoItemProps {
  title: string;
  description: string;
  size?: 'small' | 'medium' | 'large';
}

export function BentoGrid({ items }: { items: BentoItemProps[] }) {
  return (
    <div className="bento-grid">
      {items.map((item, idx) => (
        <div key={idx} className={`bento-item ${item.size || 'medium'}`}>
          <h3>{item.title}</h3>
          <p>{item.description}</p>
        </div>
      ))}
    </div>
  );
}
```

```css
.bento-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  grid-auto-rows: 200px;
  gap: 1rem;
}

.bento-item {
  background: #f5f5f5;
  padding: 1.5rem;
  border-radius: 12px;
  display: flex;
  flex-direction: column;
  justify-content: center;
}

.bento-item.small {
  grid-column: span 1;
  grid-row: span 1;
}

.bento-item.medium {
  grid-column: span 2;
  grid-row: span 1;
}

.bento-item.large {
  grid-column: span 2;
  grid-row: span 2;
}

@media (max-width: 768px) {
  .bento-item.medium,
  .bento-item.large {
    grid-column: span 1;
    grid-row: span 1;
  }
}
```

---

## 실전 예시: 페이지 구조

```tsx
export function HomePage() {
  return (
    <>
      <Header />
      <HeroSection />

      {/* 반복 컴포넌트 */}
      <BentoGrid items={features} />

      {/* 2-Column 섹션 */}
      <TwoColumnSection
        imageSrc="/feature1.jpg"
        imageAlt="Feature 1"
        subheading="Innovation"
        heading="Built for the future"
        details="Advanced technology that scales with your needs"
      />

      <TwoColumnSection
        imageSrc="/feature2.jpg"
        imageAlt="Feature 2"
        subheading="Security"
        heading="Enterprise-grade protection"
        details="Bank-level encryption keeps your data safe"
        reverse
      />

      <CTASection />
      <Footer />
    </>
  );
}
```

---

## 체크리스트

### 설계 단계
- [ ] 페이지를 반복 컴포넌트로 분해
- [ ] 각 컴포넌트의 props 정의
- [ ] 모바일/데스크톱 레이아웃 스케치

### 구현 단계
- [ ] CSS Grid로 기본 레이아웃 구성
- [ ] Container Queries로 컴포넌트 적응형 동작 추가
- [ ] 재사용 가능한 React 컴포넌트 작성

### 검증 단계
- [ ] 다양한 컨테이너 크기에서 테스트
- [ ] 반응형 동작 확인 (모바일/태블릿/데스크톱)
- [ ] 컴포넌트 재사용성 확인

## 참고 자료

### 핵심 문헌
- [MDN Web Docs - CSS Grid Layout](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Grid_Layout) — CSS Grid 공식 문서
- [MDN Web Docs - CSS Container Queries](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Container_Queries) — Container Queries 공식 문서
- [Every Layout](https://every-layout.dev/) — 재사용 가능한 레이아웃 패턴 모음

### 실제 사례
- [Mobbin](https://mobbin.com/) — 모바일/웹 디자인 패턴 아카이브, 반복 컴포넌트 분석에 유용
- [Shopify Polaris](https://polaris.shopify.com/components) — 재사용 가능한 컴포넌트 라이브러리 사례

> **실제 사례**: BBC News는 컴포넌트 기반 아키텍처로 전환하면서 200개 이상의 페이지 템플릿을 50개의 재사용 가능한 컴포넌트로 통합했습니다. 이를 통해 신규 페이지 개발 시간이 2주에서 3일로 단축되었습니다.

---

<CrossRef
  related={[
    { path: "/docs/category/03-component-design", label: "Component Design" },
    { path: "/docs/category/08-scaling-architecture", label: "Scaling & Architecture" },
    { path: "/docs/category/02-visual-foundations", label: "Visual Foundations" },
  ]}
/>
