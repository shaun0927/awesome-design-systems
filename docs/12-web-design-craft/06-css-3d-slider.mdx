---
title: "CSS 3D 이미지 슬라이더 효과 구현 정리 - Create Crazy 3D Image Slider Effects Using CSS Only"
sidebar_label: "CSS 3D Slider"
sidebar_position: 6
audience: developer
tags: ["CSS", "3D", "slider", "animations", "transforms"]
---

import DevQuickStart from '@site/src/components/DevQuickStart';
import CrossRef from '@site/src/components/CrossRef';
import BrowserSupport from '@site/src/components/BrowserSupport';

<DevQuickStart
  what="CSS 3D 슬라이더는 perspective, transform-style, backface-visibility로 구현됩니다"
  learn="CSS 3D Transform, perspective, 접근성 애니메이션"
  able="CSS만으로 3D 카드 플립/슬라이더를 구현할 수 있습니다"
/>

## 핵심 CSS 속성

### 3D 공간 활성화

```css
.slider {
  /* 3D 공간 활성화 (필수) */
  transform-style: preserve-3d;

  /* 원근감 설정 (거리감) */
  perspective: 1000px;
}
```

**perspective 값의 의미:**
- `500px`: 극적인 왜곡 (가까운 거리)
- `1000px`: 균형잡힌 깊이감 (권장)
- `2000px`: 부드러운 원근감 (먼 거리)

<BrowserSupport
  chrome="36"
  firefox="16"
  safari="9"
  edge="12"
/>

---

## 3D 원형 슬라이더 구현

### HTML 구조

```tsx
interface SliderProps {
  items: { src: string; alt: string }[];
}

export function Slider3D({ items }: SliderProps) {
  return (
    <div className="banner">
      <div className="slider" style={{ '--quantity': items.length }}>
        {items.map((item, idx) => (
          <div
            key={idx}
            className="item"
            style={{ '--position': idx + 1 }}
          >
            <img src={item.src} alt={item.alt} />
          </div>
        ))}
      </div>
    </div>
  );
}
```

### CSS 구현

```css
.banner {
  width: 100%;
  height: 100vh;
  overflow: hidden;
  position: relative;
  display: flex;
  justify-content: center;
  align-items: center;
}

.slider {
  position: absolute;
  width: 200px;
  height: 250px;
  top: 10%;
  left: calc(50% - 100px);

  /* 3D 공간 활성화 */
  transform-style: preserve-3d;
  perspective: 1000px;

  /* 아이템 총 개수 선언 */
  --quantity: 10;

  /* 무한 회전 애니메이션 */
  animation: autoRun 20s linear infinite;
}

@keyframes autoRun {
  from {
    transform: perspective(1000px) rotateY(0deg);
  }
  to {
    transform: perspective(1000px) rotateY(360deg);
  }
}

/* 개별 아이템 배치 (원형 배열) */
.item {
  position: absolute;
  inset: 0; /* top: 0; left: 0; right: 0; bottom: 0; */

  /* Z축 거리 = 원의 반지름 */
  /* Y축 회전 = 원형 배치 */
  transform:
    translateZ(550px)
    rotateY(calc((var(--position) - 1) * (360deg / var(--quantity))));
}

.item img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  border-radius: 8px;
}
```

**공식 해설:**
```
회전 각도 = (위치 - 1) × (360° / 전체 개수)

예: 10개 아이템 기준
- 아이템 1: (1-1) × 36° = 0°
- 아이템 2: (2-1) × 36° = 36°
- 아이템 3: (3-1) × 36° = 72°
```

---

## 3D 카드 플립

```tsx
export function FlipCard({ front, back }: { front: ReactNode; back: ReactNode }) {
  const [isFlipped, setIsFlipped] = useState(false);

  return (
    <div className="flip-card" onClick={() => setIsFlipped(!isFlipped)}>
      <div className={`flip-card-inner ${isFlipped ? 'flipped' : ''}`}>
        <div className="flip-card-front">
          {front}
        </div>
        <div className="flip-card-back">
          {back}
        </div>
      </div>
    </div>
  );
}
```

```css
.flip-card {
  width: 300px;
  height: 400px;
  perspective: 1000px;
}

.flip-card-inner {
  position: relative;
  width: 100%;
  height: 100%;
  transform-style: preserve-3d;
  transition: transform 0.6s;
}

.flip-card-inner.flipped {
  transform: rotateY(180deg);
}

.flip-card-front,
.flip-card-back {
  position: absolute;
  width: 100%;
  height: 100%;
  backface-visibility: hidden; /* 뒷면 숨김 */
  border-radius: 12px;
  padding: 2rem;
}

.flip-card-front {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
}

.flip-card-back {
  background: #fff;
  transform: rotateY(180deg);
}
```

---

## 접근성 애니메이션

```css
@media (prefers-reduced-motion: reduce) {
  .slider {
    animation: none; /* 애니메이션 중지 */
  }

  .flip-card-inner {
    transition: none; /* 전환 효과 제거 */
  }
}
```

**이유:** 전정 장애가 있는 사용자를 위한 필수 설정

---

## 성능 최적화

```css
.slider,
.flip-card-inner {
  /* GPU 가속 활성화 */
  will-change: transform;

  /* 뒷면 렌더링 방지 */
  backface-visibility: hidden;

  /* 레이어 프로모션 */
  transform: translateZ(0);
}
```

---

## 텍스트 윤곽선 오버레이

```tsx
<h1 data-content="ALPHAVIEW">ALPHAVIEW</h1>
```

```css
h1 {
  position: relative;
  font-size: 8rem;
  font-weight: 900;
  color: #25283b;
}

/* 텍스트 윤곽선 버전 */
h1::after {
  content: attr(data-content); /* HTML 속성값 가져오기 */
  position: absolute;
  inset: 0;
  z-index: 2;

  /* 윤곽선만 표시 */
  -webkit-text-stroke: 2px white;
  color: transparent;
}
```

---

## 수직/수평 각도 제어

```css
/* 수평 회전만 */
@keyframes autoRun {
  to { transform: perspective(1000px) rotateY(360deg); }
}

/* 수평 + 수직 회전 */
@keyframes autoRun {
  to {
    transform:
      perspective(1000px)
      rotateY(360deg)
      rotateX(15deg); /* 위에서 내려다보는 각도 */
  }
}
```

---

## 실전 사용 예시

### Hero 섹션 3D 카드

```tsx
export function HeroCards() {
  const features = [
    { icon: '🚀', title: 'Fast', description: 'Lightning speed' },
    { icon: '🔒', title: 'Secure', description: 'Bank-level encryption' },
    { icon: '📱', title: 'Mobile', description: 'Works everywhere' },
  ];

  return (
    <div className="hero-cards">
      {features.map((feature, idx) => (
        <FlipCard
          key={idx}
          front={
            <div className="text-center">
              <div className="text-6xl mb-4">{feature.icon}</div>
              <h3 className="text-2xl font-bold">{feature.title}</h3>
            </div>
          }
          back={
            <div>
              <h3 className="text-xl font-bold mb-2">{feature.title}</h3>
              <p>{feature.description}</p>
            </div>
          }
        />
      ))}
    </div>
  );
}
```

---

## 체크리스트

### 기본 설정
- [ ] `transform-style: preserve-3d` 선언 (부모)
- [ ] `perspective` 값 설정 (1000px 권장)
- [ ] CSS 변수 `--quantity` 선언 (아이템 총 개수)
- [ ] HTML에서 각 아이템에 `--position` 값 할당

### 3D 배치
- [ ] `translateZ`로 원의 반지름 설정 (550px 권장)
- [ ] `rotateY` 공식 적용
- [ ] `position: absolute` + `inset: 0`으로 크기 통일

### 애니메이션
- [ ] 슬라이더 컨테이너에 애니메이션 적용
- [ ] `animation: linear infinite` 설정
- [ ] 애니메이션 내에서 `perspective` 값 유지

### 접근성
- [ ] `@media (prefers-reduced-motion)` 처리
- [ ] `backface-visibility: hidden` 설정
- [ ] `will-change: transform` 성능 힌트

---

<CrossRef
  related={[
    { path: "/docs/category/02-visual-foundations", label: "Visual Foundations" },
    { path: "/docs/category/03-component-design", label: "Component Design" },
    { path: "/docs/category/06-accessibility", label: "Accessibility" },
  ]}
/>
